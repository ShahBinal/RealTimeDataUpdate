Install the required dependencies or libraries.

	npm install --save express
	npm install --save ws

To Run Application

	run command on terminal

	node index.js and open a browser & point to http://localhost/html/index.html